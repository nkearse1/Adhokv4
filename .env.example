# Supabase Configuration
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Service Role Key (Required for setup script)
# Get this from your Supabase project dashboard > Settings > API
# This key has full access to your database and bypasses RLS
# NEVER expose this in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Instructions:
# 1. Click "Connect to Supabase" button in the top right
# 2. Select or create your Supabase project
# 3. The environment variables will be automatically configured
# 4. Add your SUPABASE_SERVICE_ROLE_KEY from the Supabase dashboard